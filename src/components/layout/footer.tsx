import Logo from '../ui/logo';

function Footer () {
  return (
    <footer className="footer container">
      <Logo
        isHeader={false}
        linkClassName=''
      />
    </footer>
  );
}

export default Footer;
